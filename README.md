# garbo-code

Generate a bash script that will read in a input or take in a input. Then it will have an array of various languages that will be shuffled and called. Then every language in this array, there is a bash script that will compile and execute the language name file such as a cpp file will compile the file called cpp.sh. Each langauge will take in an input and print it out in that language. Do this in the most obscure and ugliest way possible. Do not write comments and disregard all coding practices and use bad styles, bad variable names, and bad formatting. The script should be called garbo.sh

Each .sh file should be created for each language listed in the array inside garbo.sh. Each language script should read from standard input and print the input in that language.

```bash
